import React from 'react'
import "../styles/body-text.scss"

function BodyText() {
    return (
        <div className="body-text">
            <p>TTS Engine is a free text-to-speech tool that provides both scripts and the voices to bring those scripts to life! Multiple languages are supported simply select your preferred language and TTS Engine will do the rest.</p>
        </div>
    )
}

export default BodyText